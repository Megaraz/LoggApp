namespace AppLogic.Interfaces
{
    // Not implemented yet, but this interface is intended to represent a log entry that contains a comment.
    internal interface ICommentLogEntry
    {
        string Comment { get; }
    }
}
